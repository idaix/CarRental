from django.urls import path
from . import views

urlpatterns = [
    # path('add_vehicle/', views.add_vehicle, name='add_vehicle'),
    # path('model_field/', views.model_field, name='model_field'),
    # path('<int:pk>', views.vehicle_details, name='vehicle_details'),
    # path('<int:pk>/update', views.update_vehicle, name='vehicle_update'),
    # path('<int:pk>/delete', views.VehicleDelete.as_view(), name='vehicle_delete'),
]